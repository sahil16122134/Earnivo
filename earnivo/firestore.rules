rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All task, wallet, submission, transaction and administration operations
    // are evaluated by the Cloudflare Worker using Firebase Admin privileges.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}


/** Ledger Light admin: transaction history is read-only and supports reconciliation without a direct balance editor. */
import { requireAdmin } from "./admin-auth.js";import { adminApi } from "./admin-api.js";import { mountAdminNav } from "./admin-nav.js";import { $,currency,escapeHtml,formatDate,statusClass } from "../../js/utils.js";
const root=$("#admin-root");async function init(){try{if(!await requireAdmin())return;mountAdminNav("transactions");const {items=[]}=await adminApi.list("transactions");root.className="";root.innerHTML=`<section class="admin-heading"><div><p class="eyebrow">Transactions</p><h1>Ledger history.</h1><p>Wallet records are created by verified approvals and withdrawal events, not by manual balance adjustment.</p></div></section><div class="table-wrap"><table><thead><tr><th>Date</th><th>User</th><th>Type</th><th>Reference</th><th>Status</th><th>Amount</th></tr></thead><tbody>${items.map(item=>`<tr><td>${formatDate(item.createdAt)}</td><td>${escapeHtml(item.userId)}</td><td>${escapeHtml(item.type)}</td><td>${escapeHtml(item.reference||"—")}</td><td><span class="${statusClass(item.status)}">${escapeHtml(item.status)}</span></td><td><strong>${currency(item.amount)}</strong></td></tr>`).join("")||"<tr><td colspan='6'>No transaction records have been created.</td></tr>"}</tbody></table></div>`}catch(error){root.className="empty-state";root.innerHTML=`<h2>Transactions could not be loaded.</h2><p>${escapeHtml(error.message)}</p>`}}init();


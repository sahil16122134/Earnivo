/** Ledger Light admin: notifications are recipient-scoped inbox records, never broad collections filtered in the browser. */
import { requireAdmin } from "./admin-auth.js";import { adminApi } from "./admin-api.js";import { mountAdminNav } from "./admin-nav.js";import { $,escapeHtml,formatDate,setLoading } from "../../js/utils.js";import { toast } from "../../js/components/toast.js";
const root=$("#admin-root");async function init(){try{if(!await requireAdmin())return;mountAdminNav("notifications");const {items=[]}=await adminApi.list("notifications");root.className="";root.innerHTML=`<section class="admin-heading"><div><p class="eyebrow">Notifications</p><h1>Member communication.</h1><p>Each notification is written to one recipient’s inbox and fetched through that member’s direct feed.</p></div></section><section class="admin-record-card"><form id="notification-form" class="admin-form-grid"><label>Title<input name="title" required maxlength="120"/></label><label>Recipient user ID<input name="userId" required maxlength="128"/></label><label>Category<select name="category"><option value="general">General</option><option value="task">Task</option><option value="wallet">Wallet</option><option value="withdrawal">Withdrawal</option><option value="referral">Referral</option><option value="account">Account</option></select></label><label class="full">Message<textarea name="body" required rows="3" maxlength="1000"></textarea></label><p class="field-note full">For multiple members, create one inbox notification per recipient through an approved operational workflow.</p><div class="full"><button class="button button--primary" type="submit">Send notification <span>→</span></button></div></form></section><div class="table-wrap"><table><thead><tr><th>Title</th><th>Category</th><th>Recipient</th><th>Created</th><th>Message</th></tr></thead><tbody>${items.map(item=>`<tr><td><strong>${escapeHtml(item.title)}</strong></td><td>${escapeHtml(item.category||"general")}</td><td>${escapeHtml(item.userId)}</td><td>${formatDate(item.createdAt)}</td><td>${escapeHtml(item.body)}</td></tr>`).join("")||"<tr><td colspan='5'>No notifications have been sent.</td></tr>"}</tbody></table></div>`;$("#notification-form",root).addEventListener("submit",async(event)=>{event.preventDefault();const f=event.currentTarget,b=$("button",f);if(!f.reportValidity())return;try{setLoading(b,true,"Sending…");await adminApi.create("notifications",Object.fromEntries(new FormData(f)));toast("Inbox notification recorded.","success");init()}catch(error){toast(error.message,"error")}finally{setLoading(b,false)}})}catch(error){root.className="empty-state";root.innerHTML=`<h2>Notifications could not be loaded.</h2><p>${escapeHtml(error.message)}</p>`}}init();

/** Ledger Light admin: audit logs provide accountability for Worker-side privileged events. */
import { requireAdmin } from "./admin-auth.js";import { adminApi } from "./admin-api.js";import { mountAdminNav } from "./admin-nav.js";import { $,escapeHtml,formatDate } from "../../js/utils.js";
const root=$("#admin-root");async function init(){try{if(!await requireAdmin())return;mountAdminNav("logs");const {items=[]}=await adminApi.list("logs");root.className="";root.innerHTML=`<section class="admin-heading"><div><p class="eyebrow">Audit log</p><h1>Recorded actions.</h1><p>System and administrator events are retained as contextual records for review.</p></div></section><div class="table-wrap"><table><thead><tr><th>Time</th><th>Actor</th><th>Event</th><th>Details</th></tr></thead><tbody>${items.map(item=>`<tr><td>${formatDate(item.createdAt,{month:"short",day:"numeric",year:"numeric",hour:"numeric",minute:"numeric"})}</td><td>${escapeHtml(item.actorId||"system")}</td><td><strong>${escapeHtml(item.event||"—")}</strong></td><td>${escapeHtml(JSON.stringify(item.details||{}))}</td></tr>`).join("")||"<tr><td colspan='4'>No audit entries have been recorded.</td></tr>"}</tbody></table></div>`}catch(error){root.className="empty-state";root.innerHTML=`<h2>Audit logs could not be loaded.</h2><p>${escapeHtml(error.message)}</p>`}}init();

/** Ledger Light admin: ongoing work is a focused filter of submissions still awaiting a final outcome. */
import { requireAdmin } from "./admin-auth.js";import { adminApi } from "./admin-api.js";import { mountAdminNav } from "./admin-nav.js";import { $,currency,escapeHtml,formatDate,statusClass } from "../../js/utils.js";
const root=$("#admin-root");async function init(){try{if(!await requireAdmin())return;mountAdminNav("ongoing");const {items=[]}=await adminApi.list("submissions");const ongoing=items.filter(item=>["in_progress","verification"].includes(item.status));root.className="";root.innerHTML=`<section class="admin-heading"><div><p class="eyebrow">Ongoing tasks</p><h1>Unresolved work.</h1><p>These submissions are still in progress or waiting for verification.</p></div></section><div class="table-wrap"><table><thead><tr><th>Task</th><th>User</th><th>Started</th><th>Reward</th><th>Status</th></tr></thead><tbody>${ongoing.map(item=>`<tr><td><strong>${escapeHtml(item.taskTitle||item.taskId)}</strong></td><td>${escapeHtml(item.userId)}</td><td>${formatDate(item.startedAt)}</td><td>${currency(item.reward)}</td><td><span class="${statusClass(item.status)}">${escapeHtml(item.status)}</span></td></tr>`).join("")||"<tr><td colspan='5'>No submissions are currently unresolved.</td></tr>"}</tbody></table></div>`}catch(error){root.className="empty-state";root.innerHTML=`<h2>Ongoing records could not be loaded.</h2><p>${escapeHtml(error.message)}</p>`}}init();

